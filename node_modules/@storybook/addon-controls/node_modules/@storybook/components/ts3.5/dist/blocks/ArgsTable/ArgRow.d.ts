import { FC } from 'react';
import { TableArgType, Args } from './types';
export interface ArgRowProps {
    row: TableArgType;
    arg: any;
    updateArgs?: (args: Args) => void;
    compact?: boolean;
}
export declare const ArgRow: FC<ArgRowProps>;
